name: CI

on:
  workflow_dispatch:
  workflow_call:

jobs:
  release:
    environment: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/prepare
      - run: bun run release
